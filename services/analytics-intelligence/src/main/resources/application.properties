# Analytics Intelligence Service Configuration
quarkus.application.name=analytics-intelligence-service
quarkus.http.port=8081

# Database Configuration - Production
quarkus.datasource.db-kind=postgresql
quarkus.datasource.username=${DB_USERNAME:analytics_user}
quarkus.datasource.password=${DB_PASSWORD:analytics_pass}
quarkus.datasource.reactive.url=${DB_URL:postgresql://localhost:5432/analytics_db}

# Hibernate Reactive Configuration
quarkus.hibernate-orm.database.generation=update
quarkus.hibernate-orm.log.sql=true
quarkus.hibernate-orm.packages=com.chiro.erp.analyticsintelligence

# Development Configuration - disable database completely
%dev.quarkus.hibernate-orm.active=false
%dev.quarkus.datasource.active=false

# Health Check Configuration
quarkus.smallrye-health.ui.always-include=true

# Logging Configuration
quarkus.log.level=INFO

# Test Configuration - disable database for tests
%test.quarkus.hibernate-orm.active=false
%test.quarkus.datasource.active=false
quarkus.log.category."com.chiro.erp.analyticsintelligence".level=DEBUG
